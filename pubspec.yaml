name: mufakkirati_journal
description: A professional Arabic journal app named مفكرتي built with Flutter.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=2.19.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.6
  shared_preferences: ^2.2.1
  fl_chart: any
  google_fonts: ^4.0.4
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true
